# 调用方法

本文说明如何在 Codex 和 Claude Code 中调用 `gongwen_format_V1_3_1` skill。

该 skill 的核心能力由 `SKILL.md`、`assets/default_template.json`、`scripts/format_gongwen.py`、`scripts/inspect_docx.py` 和 `references/formatting_rules.md` 组成。不同工具的差异主要在安装目录、触发方式和是否支持 slash 命令。

## 1. 在 Codex 中调用

### 1.1 安装位置

Codex 的个人 skill 目录通常为：

```text
C:\Users\用户名\.codex\skills
```

本 skill 安装后应位于：

```text
C:\Users\用户名\.codex\skills\gongwen_format_V1_3_1
```

目录中应包含：

```text
SKILL.md
assets\
references\
scripts\
```

### 1.2 推荐调用方式

Codex 中通常不需要使用 slash 命令。推荐直接用自然语言说明要使用该 skill。

示例：

```text
请用 gongwen_format_V1_3_1 skill，把 D:\000临时\输入.txt 生成规范公文格式 docx，输出到 D:\000临时。
```

也可以这样写：

```text
请用公文排版 skill，把 D:\000临时\材料.docx 重新排版成规范公文格式。
```

或者：

```text
使用 gongwen_format_V1_3_1，把下面文字生成公文格式 Word 文档：

这里粘贴正文内容……
```

### 1.3 可触发 skill 的常见表达

以下表达通常可以触发该 skill：

- 公文排版
- 材料排版
- 按公文格式
- 生成 Word/docx
- 清除原格式
- 标题空一行
- 正文两端对齐
- 中英文标点适配
- 重点字段强调
- 验证生成的 docx 排版

### 1.4 Codex 中的注意事项

- skill 不是普通命令行程序中的 `/命令`。
- 如果刚安装或更新 skill，建议重启 Codex 后再使用。
- 如果自然语言没有触发，可以明确写出 `gongwen_format_V1_3_1 skill`。
- 如果需要指定文件路径，尽量使用完整路径。

## 2. 在 Claude Code 中调用

### 2.1 安装位置

Claude Code 的个人 skill 目录通常为：

```text
C:\Users\用户名\.claude\skills
```

本 skill 安装后应位于：

```text
C:\Users\用户名\.claude\skills\gongwen_format_V1_3_1
```

可用以下 PowerShell 命令复制：

```powershell
New-Item -ItemType Directory -Force "$env:USERPROFILE\.claude\skills"
Copy-Item -Recurse -Force "你的skill发布目录\gongwen_format_V1_3_1" "$env:USERPROFILE\.claude\skills\"
```

### 2.2 slash 命令调用

Claude Code 会把 skill 目录名作为 slash 命令名。

安装后可以在 Claude Code 中输入：

```text
/gongwen_format_V1_3_1 把 D:\000临时\输入.txt 按公文格式排版，输出到 D:\000临时。
```

也可以处理 Word 文档：

```text
/gongwen_format_V1_3_1 把 D:\000临时\材料.docx 重新生成规范公文格式 docx。
```

### 2.3 自然语言调用

Claude Code 也可以通过自然语言触发：

```text
请用公文排版 skill，把这份文档生成规范公文格式。
```

或者：

```text
请使用 gongwen_format_V1_3_1，整理这段文字并生成 docx。
```

### 2.4 Claude Code 中的注意事项

- slash 命令必须在输入开头使用 `/`。
- 如果输入 `/` 后看不到 `gongwen_format_V1_3_1`，请确认 skill 是否安装到 `.claude\skills` 目录。
- 如果刚创建 `.claude\skills` 或刚复制 skill，建议重启 Claude Code。
- 如果 slash 命令不可见，也可以让 Claude Code 读取 `SKILL.md` 并直接调用 `scripts/format_gongwen.py`。

## 3. 直接调用脚本

如果工具没有自动识别 skill，也可以直接调用脚本。

从 `.txt` 生成：

```powershell
python "$env:USERPROFILE\.codex\skills\gongwen_format_V1_3_1\scripts\format_gongwen.py" --text-file "D:\000临时\输入.txt" --output-dir "D:\000临时"
```

从 `.docx` 生成：

```powershell
python "$env:USERPROFILE\.codex\skills\gongwen_format_V1_3_1\scripts\format_gongwen.py" --input "D:\000临时\输入.docx" --output-dir "D:\000临时"
```

检查生成结果：

```powershell
python "$env:USERPROFILE\.codex\skills\gongwen_format_V1_3_1\scripts\inspect_docx.py" "D:\000临时\输入_排版结果.docx"
```

如果在 Claude Code 目录中安装，则把脚本路径中的 `.codex\skills` 换成 `.claude\skills`。

## 4. 常见问题

### 为什么在 Codex 输入 `/gongwen_format_V1_3_1` 没反应？

Codex 中 skill 通常不是 slash 命令。请改用自然语言，例如：

```text
请用 gongwen_format_V1_3_1 skill，把这份 txt 生成规范公文格式 docx。
```

### 为什么在 Claude Code 看不到 `/gongwen_format_V1_3_1`？

常见原因：

- skill 没有复制到 `C:\Users\用户名\.claude\skills`。
- skill 目录名不是 `gongwen_format_V1_3_1`。
- 目录里缺少 `SKILL.md`。
- Claude Code 没有重启。

### 是否必须用 slash 命令？

不是。只要工具能读取 `SKILL.md` 并运行脚本，就可以通过自然语言或直接脚本调用完成同样功能。

### 输入文件会被覆盖吗？

不会。skill 会生成新的 `.docx` 文件，默认文件名会带有“排版结果”。如果同名文件已存在，会自动追加序号。
