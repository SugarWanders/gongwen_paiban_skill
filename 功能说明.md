# 功能说明

本文说明 `gongwen_format_V1_3_1` skill 可以实现的主要功能。该 skill 用于在支持本地 skill、规则文档或脚本调用的 AI 编程工具中辅助生成规范化的公文排版 `.docx` 文件，例如 Codex、Claude Code 等。默认规则以 skill 目录中的模板和脚本为准，与 EXE 应用分别维护。

## 工具兼容性

该 skill 的核心能力由 Markdown 规则、JSON 模板和 Python 脚本组成，不绑定单一工具。

- 如果工具支持本地 skill 目录，可以按该工具的目录规范安装 `gongwen_format_V1_3_1/`。
- 如果工具不支持自动识别 skill，也可以让工具读取 `SKILL.md`、`功能说明.md` 和 `references/formatting_rules.md`，再调用 `scripts/format_gongwen.py` 生成文档。
- 在 Codex、Claude Code 等不同工具中使用时，实际差异主要是触发方式、安装目录和上下文读取方式，排版脚本本身保持一致。

## 1. 输入内容整理

skill 支持处理以下输入：

- 用户直接粘贴的正文内容。
- `.txt` 纯文本文件。
- `.docx` Word 文档中的正文文本。

处理 `.docx` 时，skill 会读取文档中的文字内容，并按 skill 的排版规则重新生成新的 `.docx` 文件。原文件不会被覆盖。

## 2. 生成 Word 文档

skill 可以把输入内容生成标准 Word 文档，输出格式为 `.docx`。

默认生成内容包括：

- A4 页面。
- 固定页边距。
- 固定行距。
- 文档标题样式。
- 一级标题样式。
- 二级标题样式。
- 正文样式。
- 标题后自动增加一个空白段落。
- 正文段落两端对齐。
- 正文段落首行缩进。

输出文件默认以文档标题命名，并在文件名后追加“排版结果”。如果同名文件已存在，skill 会自动追加序号，避免覆盖已有文件。

## 3. 标题和正文层级识别

skill 会根据文本结构生成不同段落类型：

- 第一段作为文档标题处理。
- 符合一级标题特征的段落按一级标题样式处理。
- 符合二级标题特征的段落按二级标题样式处理。
- 其他内容按正文样式处理。

这种处理适合常见的材料、报告、通知、总结、发言稿、汇报稿等文本排版场景。

## 4. 重点字段强调

skill 支持对正文中的重点字段进行自动匹配和强调。

默认重点字段包括：

- 分析认为
- 一是
- 二是
- 三是
- 四是
- 五是
- 六是
- 七是
- 八是
- 九是
- 十是

用户可以在当前任务中临时指定重点字段：

- 替换默认重点字段列表。
- 在默认重点字段基础上追加新字段。
- 调整重点字段字体。
- 调整重点字段字号。
- 设置重点字段是否加粗。

skill 只会强调正文中匹配到的字段，不会改变未匹配内容。

## 5. 中英文标点自动整理

skill 可以根据上下文自动整理中英文标点。

主要能力包括：

- 中文语境使用中文标点。
- 英文语境保留英文标点。
- 中文句子中夹带英文产品名或英文缩写时，句末标点按中文语境处理。
- 保护数字序号和小数点，避免误转换。

如果用户明确要求不整理标点，skill 可以关闭该处理。

## 6. 临时排版参数覆盖

用户可以在一次任务中临时修改排版参数，未指定的项目继续使用默认规则。

支持临时调整的内容包括：

- 页面边距。
- 行距。
- 标题字体和字号。
- 一级标题字体和字号。
- 二级标题字体和字号。
- 正文字体和字号。
- 重点字段列表。
- 重点字段字体、字号和加粗状态。
- 输出目录或输出文件路径。
- 是否启用标点自动整理。

这些临时调整只影响当前生成任务，不会要求用户手动编辑 skill 文件。

## 7. 本地字体匹配和提示

skill 运行时会读取用户电脑中已经安装的字体，并在生成 `.docx` 时写入对应字体名称。

对于需要匹配的标题字体，skill 会尝试从本地字体名称中寻找合适候选项，并在生成后报告实际使用的字体名称。

如果某个字体在本地没有找到，skill 会继续生成文档，同时给出提示，方便用户决定是否安装对应字体或指定替代字体。

## 8. 结果检查

skill 附带检查脚本，可用于快速查看生成文档的关键排版信息。

可检查的内容包括：

- 文档标题文本。
- 标题是否居中。
- 标题后是否存在空白段落。
- 前若干段落的字体、字号、加粗和对齐方式。

该检查用于辅助确认生成结果是否符合预期。

## 9. 适用场景

skill 适合以下场景：

- 把普通文本整理成公文格式 Word 文档。
- 把材料、报告、通知、总结等内容统一排版。
- 从已有 `.docx` 中提取正文并重新排版。
- 临时调整字体、字号、边距、行距等参数后生成文档。
- 自动强调“一是、二是、三是”等重点字段。
- 生成后检查标题、空行、字体、字号等关键排版信息。

## 10. 注意事项

- skill 不会覆盖原始输入文件。
- skill 生成的是新的 `.docx` 文件。
- skill 依赖 Python 和 `python-docx` 执行文档生成。
- Word 最终显示效果会受到用户电脑本地字体环境影响。
- skill 的默认规则以 `gongwen_format_V1_3_1` 目录中的模板、脚本和参考文档为准。
